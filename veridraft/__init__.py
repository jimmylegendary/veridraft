"""Veridraft — Paper & Patent Writing Harness (v1 vertical slice).

An evidence-gated governance harness over a swappable writing engine. This package
is the first implementation slice: `gated claim → PDF`, end-to-end, on the Python
standard library only (no pip, no LaTeX, no LLM, no network required to run it).

See impl/README.md for the runbook and design/ for the ADRs this implements.
"""

__version__ = "0.0.1"
